from zeta import *
# from mpmath import *

# API

# plot functions with:
# - product_plot(re, im, ?lim)
# - abs_vs_n(re, im, ?lim)
# - drdtheta_vs_n(re, im, ?lim)

# build lists with:
# - get_zeros
# - product_spiral(re, im, ?lim) -- uses global primes for length
# - sum_spiral(re, im, ?lim) -- uses 100000 as default value
# - drdtheta(r) -- where r is a list of vector magnitudes
# - criticals(f) -- where f is a list of single-valued drdt outputs

# zeros = get_zeros(5)

abs_vs_n(0.5, 10)
